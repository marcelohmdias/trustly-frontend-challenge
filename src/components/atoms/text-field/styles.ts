import styled from 'styled-components'

export const Text = styled.p`
  color: #a5a5a5;
  font-family: Arial;
  font-size: 0.85em;
  font-style: normal;
  font-weight: normal;
  line-height: 25px;
  white-space: nowrap;
`
